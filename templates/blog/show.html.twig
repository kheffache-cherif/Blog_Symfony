{% extends 'base.html.twig' %} {# ettendre le template de base #}
 {% block body %}   {# ettendre le body de base #}
     <article>   {# je declare un seule article  #}
            <h2>{{article.title}}</h2>
            <div class="metadata">Ecrit le 12/06/2020 à 19:00 dans catégorie {{article.catégorie.title}} </div>
            <div class="content">
                <img src="{{article.image}}" alt="">
        
            </div>
        </article>

 {% endblock %}